# Gmail SMTP Configuration
# Replace with your actual Gmail credentials

# Your Gmail address (must match sender email)
mail.smtp.user=Nhap Email May chu google

# Gmail App Password (NOT your regular Gmail password)
# Generate at: https://myaccount.google.com/apppasswords
mail.smtp.key=Nhap App Password google cua ban

# Sender information (must match mail.smtp.user for Gmail)
mail.sender.email=Nhap Email Nguoi Gui (giong mail.smtp.user)
mail.sender.name=SWP Sales Process System