# Custom Agent Rules

- **No Fully Qualified Names in Code**: Do not use inline fully qualified package names like `model.CustomerOrder` or `dto.CustomerOrderDTO` in parameter lists or method bodies. Always import the required class at the top of the file and use the class name directly.
